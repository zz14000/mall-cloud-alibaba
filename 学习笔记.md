## day1

### 启动ums模块时，报错

Failed to configure a DataSource: 'url' attribute is not specified

Reason: Failed to determine a suitable driver class

原因：未使用nacos自带的sql文件初始化业务数据库，找不到数据源（sql文件地址conf/nnacos-mysql.sql）



### redis序列化

**jdk序列化**，默认配置，但不推荐

**string序列化**，存纯字符串，日常常用，`stringRedisTemplate` 默认用它

**json序列化**，存复杂类型数据，有两种实现方，Jackson2JsonRedisSerializer（JSON）和 GenericJackson2JsonRedisSerializer（通用 JSON）推荐后者，复杂类型不报错

**xml序列化**：使用 Spring [OXM](https://www.jianshu.com/p/1b7c8dc26001)实现将对象和 String 的转换，从而 String 和二进制数组的转换。基本没用过



### 网关filter工作流程

```
请求进入
  │
  ▼
是否 OPTIONS 请求？ ──是──▶ 放行
  │否
  ▼
是否在白名单中？ ──是──▶ 放行
  │否
  ▼
请求头中是否有 Token？ ──否──▶ 抛出 401
  │是
  ▼
解析 Token 获取用户名
  │
  ▼
Redis 中是否存在该用户 Token？ ──否──▶ 抛出 401
  │是
  ▼
放行，请求转发到下游服务
```



### 配置文件中JWT配置项tokenHeader 和 tokenHead 详解

配置文件代码

```yml
jwt:
  tokenHeader: Authorization  # JWT存储的请求头
  tokenHead: Bearer           # JWT负载中拿到开头
```

具体例子(前端发送的http请求)

```http
GET /admin/info HTTP/1.1
Host: localhost:8080
Authorization: Bearer eyJhbGciOiJIUzUxMiJ9.eyJzdWIiOiJhZG1pbiJ9.xxx
Content-Type: application/json
```

**tokenHeader** 的值是 Authorization ，这是 HTTP 协议中专门用于身份认证的标准请求头字段名 。在代码中，通过这个名称从 HTTP 请求中取出对应的头部值，即Bearer eyJhbGciOiJIUzUxMiJ9.eyJzdWIiOiJhZG1pbiJ9.xxx

**tokenHead** 的值是 Bearer，这是 OAuth 2.0 / JWT 的标准认证方案标识 。 Bearer 的含义是"持有者"——谁持有这个 Token，谁就拥有访问权限。HTTP 的 Authorization 头格式是： <认证方案> <凭证>。在代码中可通过这个前缀定位到真正的token内容

用一句话概括： tokenHeader 告诉系统"去哪个请求头找 Token"， tokenHead 告诉系统"Token 值以什么前缀开头，需要截掉前缀才能解析" 。

### Example 类

Example 类就是 MyBatis Generator 为每张表自动生成的"WHERE 条件构建器"，它把 SQL 的 WHERE 子句翻译成了 Java 方法的链式调用，让你用面向对象的方式拼查询条件，而不用手写 SQL。每一个模型类都有一个对应的Example 类

Example 类处理单表 CRUD（占 80% 的日常操作），手写 Dao 处理多表联查、批量操作、复杂子查询（占 20% 但不可替代）。两者不是替代关系，而是互补关系——MBG 生成的基础操作免去重复劳动，手写 SQL 解决 Example 表达不了的复杂场景。两者只是方式不一样，底层都是mybatis

```
┌──────────────────────────────────────────────────────┐
│                   Service 层                          │
│                                                      │
│   根据需求选择使用哪种方式                              │
│         │                            │                │
│         ▼                            ▼                │
│  ┌─────────────┐            ┌──────────────┐         │
│  │ MBG Mapper  │            │  手写 Dao     │         │
│  │ (单表CRUD)  │            │ (复杂查询)    │         │
│  └──────┬──────┘            └──────┬───────┘         │
│         │                          │                  │
└─────────┼──────────────────────────┼──────────────────┘
          ▼                          ▼
┌─────────────────┐        ┌─────────────────────┐
│ ums-mbg 模块     │        │ ums-server 模块      │
│                 │        │                     │
│ Mapper 接口     │        │ Dao 接口             │
│ Model + Example │        │ resources/dao/*.xml  │
│ (自动生成)      │        │ (手写SQL)            │
└────────┬────────┘        └──────────┬──────────┘
         │                            │
         ▼                            ▼
    ┌─────────────────────────────────────┐
    │          MyBatis 统一执行            │
    │    (两种方式底层都是 MyBatis)         │
    └─────────────────┬───────────────────┘
                      ▼
              ┌───────────────┐
              │   数据库       │
              └───────────────┘
```

Example类情况下service层调用链条：

构造 Example → 设置条件 → 调用 Mapper 方法 。



### Spring Security 在项目中的完整使用流程

```
┌──────────────────────────────────────────────────────────────────────┐
│                        完整数据流                                      │
└──────────────────────────────────────────────────────────────────────┘

【登录阶段】
  客户端
    │ POST /admin/login {username, password}
    ▼
  UmsAdminController.login()
    │
    ▼
  UmsAdminServiceImpl.login()
    │
    ├─▶ loadUserByUsername(username)
    │     ├─▶ getAdminByUsername(username)
    │     │     ├─▶ 查缓存 → 未命中
    │     │     └─▶ 查数据库 → UmsAdmin
    │     │           └─▶ 写入缓存
    │     │
    │     └─▶ getResourceList(adminId)
    │           ├─▶ 查缓存 → 未命中
    │           └─▶ 查数据库 (4 表联查) → List<UmsResource>
    │                 └─▶ 写入缓存
    │
    ├─▶ 构建 AdminUserDetails
    │     └─▶ getAuthorities() = resourceList 转换
    │
    ├─▶ 验证密码
    │
    ├─▶ 构建 UsernamePasswordAuthenticationToken
    │     (userDetails, null, authorities)
    │
    ├─▶ SecurityContextHolder.setAuthentication(authentication) ★
    │
    └─▶ 生成 JWT Token
          └─▶ 返回给客户端

【请求阶段】
  客户端
    │ GET /product/list
    │ Authorization: Bearer {token}
    ▼
  JwtAuthenticationTokenFilter
    │ 解析 Token → username
    │ loadUserByUsername(username) → userDetails
    │ 验证 Token
    │ 构建 UsernamePasswordAuthenticationToken
    │ SecurityContextHolder.setAuthentication(authentication) ★
    ▼
  DynamicSecurityFilter
    │ 获取请求路径 /product/list
    ▼
  DynamicSecurityMetadataSource
    │ 查询该路径需要的权限
    │ → ["2:商品查看"]
    ▼
  DynamicAccessDecisionManager.decide()
    │ 从 SecurityContextHolder.getAuthentication()
    │   获取用户权限 ["1:商品添加", "2:商品查看", "3:商品删除"]
    │ 比对："2:商品查看" 在用户权限中 ✅
    │ → 放行
    ▼
  UmsProductController.list()
    │ 执行 Controller 方法
    ▼
  返回结果
```



### 白名单放行重复？

### 问题 4：白名单放行是否重复
答 ：不是重复，是分层防御：

- 配置层 ：告诉 Spring Security 框架哪些 URL 不需要认证
- JWT 过滤器 ：没有 Token 的请求不认证
- 动态权限过滤器 ：避免对静态资源执行复杂的权限决策
三层放行的目的不同，互为补充，确保性能和安全性。



### setAuthentication 后信息的完整使用链路

具体内容在**setAuthentication 后信息的完整使用链路.md**中

内容包括 ：

1. 核心结论 - 3 个主要使用点
2. 使用点 1 - 动态权限决策（立即使用）
3. 使用点 2 - Controller 中获取当前用户信息（业务使用）
   - 场景 A：通过 Principal 参数自动注入
   - 场景 B：手动从 SecurityContextHolder 获取
   - 场景 C：Service 层获取当前用户
4. 使用点 3 - Spring Security 内部使用（隐式使用）
   - 场景 A： @PreAuthorize 注解
   - 场景 B：方法级权限控制
5. 完整使用流程图 - 从 JWT 过滤器到 Controller 的完整链路
6. 两次 setAuthentication 的对比 - 登录时 vs 请求鉴权时
7. 为什么需要 ThreadLocal - Spring MVC 单例多线程架构的必然选择
8. 总结表格 - 5 个使用时机和对应信息



### 为什么后台服务里也有 Spring Security，网关里也做 token 校验。

```
请求流程：
  客户端请求
    │
    ▼
① Gateway (AuthGlobalFilter)
    │ ✓ 校验 Token 是否存在
    │ ✓ 校验 Token 是否过期（查 Redis）
    │ ✓ 白名单放行
    │
    ▼
② 后台服务 (JwtAuthenticationTokenFilter)
    │ ✓ 再次校验 Token
    │ ✓ 解析 Token 获取用户名
    │ ✓ 加载用户信息和权限
    │ ✓ 放入 SecurityContextHolder
    │
    ▼
③ 后台服务 (DynamicSecurityFilter)
    │ ✓ 权限比对
    │
    ▼
④ Controller
```

网关：粗粒度认证，只关心token是否存在，是否过期

后端服务：细粒度授权，解析token，加载用户权限



### isBlank()函数

`org.apache.commons.lang3.StringUtils` 是 Apache 提供的**字符串工具类**，`isBlank` 是其中最常用的方法之一，专门用来判断字符串是否为**空白**。

对比isEmpty()函数，他们对于空字符，null都能判断为true，对于纯空格字符isBlank判断为true，isempty判断为false