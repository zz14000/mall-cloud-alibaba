## day1会员

### 核心表分析

主表为`ums-member`，memberLevelId字段关联`ums_member_level`会员等级表，定义会员等级体系中的各个等级及其对应权益。

主表integration积分和growth成长值字段有对应的操作记录表，`ums_integration_change_history`积分变更历史表和`ums_growth_change_history`成长值变更历史表，两张表都有memberId字段

其他辅助表

- 会员信息统计表-汇总统计会员的各项业务数据，用于会员画像构建和数据分析。
- 会员标签表 - `ums_member_tag`定义会员标签及其自动打标签规则，通过`ums_member_member_tag_relation`中间表与会员建立联系
- 会员收货地址表 - `ums_member_receive_address`，存储会员的收货地址信息，有memberId字段
- 会员登录日志表 - `ums_member_login_log`，记录前台会员每次登录的详细信息，有memberId字段
- 会员规则设置表 - `ums_member_rule_setting`，配置会员积分和成长值的获取规则
- 积分消费设置表 - `ums_integration_consume_setting`，配置会员积分在订单消费时的抵扣规则

**详细信息在ums模块下的ums核心表分析.md中**



### 获取当前用户

```Java
public UmsMember getCurrentMember() {
        SecurityContext ctx = SecurityContextHolder.getContext();
        Authentication auth = ctx.getAuthentication();
        MemberDetails memberDetails = (MemberDetails) auth.getPrincipal();
        return memberDetails.getUmsMember();
    }
```



### 前台登录和后台登录的对比

相同点：技术栈和步骤相同，都使用sprinng security框架，都使用jwt,redis

不同点：前台会员登录，权限硬编码为TEST，不缓存token，不记录登录日志

**详细对比见member模块下的前后台登录对比.md**



### mongoDB的使用-以会员关注品牌管理为例

项目使用了spring data mongoDB，因此在`MemberBrandAttentionRepository`中，**你没有写 SQL，也没有写 Mongo 查询语句，但 Spring Data 会根据方法名自动生成查询**。

#### 请求链路

**Controller 收请求 → Service 补当前会员信息 → Repository 自动翻译成 MongoDB 查询 → MemberBrandAttention 文档落到 MongoDB 集合里**。

这个功能里的 `MongoDB` 思路很简单：`MemberBrandAttention` 是文档模型，`MemberBrandAttentionRepository` 是自动生成 Mongo 操作的入口，`MemberAttentionServiceImpl` 负责把“当前登录会员”这个条件加进去，避免前端伪造会员 ID。真正和 MongoDB 打交道的地方，就是这些 Repository 方法：save、findByMemberIdAndBrandId、deleteByMemberIdAndBrandId、findByMemberId、deleteAllByMemberId。

#### 如何引入mongoDB

member-server/pom.xml 里引了：

```xml
<artifactId>spring-boot-starter-data-mongodb</artifactId>
```

然后 application-dev.yml 配了：

```yml
spring:
  data:
    mongodb:
      host: localhost
      port: 27017
      database: mall-port
```

意思是：这个 member 服务启动后，会连接本机 27017 端口的 MongoDB，并使用 mall-port 这个数据库。



## day2前台首页，商品详情

这个模块是一个 商品展示和查询模块 ，只有简单的CRUD功能，没有复杂逻辑

主要职责：

✅ 商品搜索、筛选、排序
✅ 商品详情展示
✅ 商品分类树
✅ SKU 规格查询
✅ 促销配置查询（仅展示，不计算）
✅ 库存锁定（简单版本）



## day3优惠卷

### 获取会员优惠卷列表时，已经有useStatus字段表示是否过期，为啥还需要比较时间

这种设计是一种**防御性编程**：

- useStatus 是**业务状态**
- 时间判断是**物理状态**

两者结合可以确保：

1. 即使用户状态更新不及时，也能正确过滤
2. 避免因为定时任务延迟导致的数据不一致
3. 提供更准确的查询结果

这是一种常见的最佳实践，特别是在电商系统中，优惠券的有效期管理非常重要。



## day4 订单

### generateOrder 函数解析

源码位置：

- 接口入口：`mall-portal-order/order-server/src/main/java/com/mtcarpenter/mall/portal/controller/OmsPortalOrderController.java`
- 核心实现：`mall-portal-order/order-server/src/main/java/com/mtcarpenter/mall/portal/service/impl/OmsPortalOrderServiceImpl.java`
- 入参对象：`mall-portal-order/order-server/src/main/java/com/mtcarpenter/mall/portal/domain/OrderParam.java`

这个函数是前台订单模块里最核心的“真正下单”逻辑。前面的 `generateConfirmOrder` 只是生成确认单，给用户看地址、优惠券、积分、商品金额等信息；而 `generateOrder` 会真正写入订单表、订单商品表，并且修改优惠券、积分、购物车、库存锁定状态。

一句话理解：

> 前端提交选中的购物车商品、地址、优惠券、积分、支付方式，后端重新计算价格和优惠，校验库存，锁定库存，创建订单，扣积分/改优惠券/删购物车，最后发送延迟消息用于超时取消订单。

#### 一、接口入口

Controller 中的入口是：

```java
@ApiOperation("根据购物车信息生成订单")
@RequestMapping(value = "/generateOrder", method = RequestMethod.POST)
@ResponseBody
public CommonResult generateOrder(@RequestBody OrderParam orderParam) {
    Map<String, Object> result = portalOrderService.generateOrder(orderParam);
    return CommonResult.success(result, "下单成功");
}
```

也就是说，前端调用：

```text
POST /order/generateOrder
```

请求体是 `OrderParam`。

#### 二、OrderParam 入参含义

```java
public class OrderParam {
    private Long memberReceiveAddressId;
    private Long couponId;
    private Integer useIntegration;
    private Integer payType;
    private List<Long> cartIds;
}
```

字段含义：

| 字段 | 含义 |
| --- | --- |
| `memberReceiveAddressId` | 收货地址 ID，用来查询会员收货地址 |
| `couponId` | 用户选择使用的优惠券 ID，不传表示不用优惠券 |
| `useIntegration` | 用户选择使用的积分数量，不传或为 0 表示不用积分 |
| `payType` | 支付方式，源码注释里是 `0->未支付；1->支付宝；2->微信` |
| `cartIds` | 被选中的购物车记录 ID，只给这些购物车商品生成订单 |

这里要注意：前端只传“选择了什么”，比如选了哪些购物车商品、哪个地址、哪个优惠券、多少积分。真正的商品价格、促销金额、优惠券可用性、库存，都要后端重新计算，不能相信前端传来的金额。

#### 三、先准备订单商品集合

函数一开始创建了一个空的订单商品列表：

```java
List<OmsOrderItem> orderItemList = new ArrayList<>();
```

`OmsOrderItem` 对应的是订单明细，也就是订单里的每一件商品。后面会把购物车商品转换成订单商品快照。

#### 四、获取当前会员和购物车促销信息

```java
UmsMember currentMember = memberUtil.getRedisUmsMember(request);
List<CartPromotionItem> cartPromotionItemList =
        cartItemService.listPromotion(currentMember.getId(), orderParam.getCartIds());
```

这里做了两件事：

1. 从 Redis 或当前请求中拿当前登录会员 `currentMember`。
2. 根据当前会员 ID 和前端提交的 `cartIds`，重新查询购物车商品，并计算每个购物车商品的促销信息。

`CartPromotionItem` 不是普通购物车对象，它继承了 `OmsCartItem`，并额外带了这些促销字段：

```java
private String promotionMessage;
private BigDecimal reduceAmount;
private Integer realStock;
private Integer integration;
private Integer growth;
```

含义：

| 字段 | 含义 |
| --- | --- |
| `promotionMessage` | 当前商品命中的促销描述 |
| `reduceAmount` | 当前商品每件减少的促销金额 |
| `realStock` | 真实可用库存，通常是库存减去锁定库存 |
| `integration` | 购买该商品赠送的积分 |
| `growth` | 购买该商品赠送的成长值 |

这一步非常关键：下单前不是直接拿购物车原始价格，而是通过 `cartItemService.listPromotion(...)` 重新计算促销，保证订单使用的是服务端最新规则。

#### 五、把购物车商品转换成订单商品

源码中遍历 `cartPromotionItemList`，为每个购物车商品创建一个 `OmsOrderItem`：

```java
for (CartPromotionItem cartPromotionItem : cartPromotionItemList) {
    OmsOrderItem orderItem = new OmsOrderItem();
    orderItem.setProductId(cartPromotionItem.getProductId());
    orderItem.setProductName(cartPromotionItem.getProductName());
    orderItem.setProductPic(cartPromotionItem.getProductPic());
    orderItem.setProductAttr(cartPromotionItem.getProductAttr());
    orderItem.setProductBrand(cartPromotionItem.getProductBrand());
    orderItem.setProductSn(cartPromotionItem.getProductSn());
    orderItem.setProductPrice(cartPromotionItem.getPrice());
    orderItem.setProductQuantity(cartPromotionItem.getQuantity());
    orderItem.setProductSkuId(cartPromotionItem.getProductSkuId());
    orderItem.setProductSkuCode(cartPromotionItem.getProductSkuCode());
    orderItem.setProductCategoryId(cartPromotionItem.getProductCategoryId());
    orderItem.setPromotionAmount(cartPromotionItem.getReduceAmount());
    orderItem.setPromotionName(cartPromotionItem.getPromotionMessage());
    orderItem.setGiftIntegration(cartPromotionItem.getIntegration());
    orderItem.setGiftGrowth(cartPromotionItem.getGrowth());
    orderItemList.add(orderItem);
}
```

这一步可以理解为“购物车商品快照变成订单商品快照”。

为什么要复制这么多商品字段？

因为订单生成以后，商品名称、图片、价格、SKU、促销信息都应该按下单时的状态保存下来。哪怕后面商品改名、改图、改价，已经生成的订单也不能被影响。

这里几个重要字段：

| 订单明细字段 | 来源 | 作用 |
| --- | --- | --- |
| `productPrice` | `cartPromotionItem.getPrice()` | 下单时商品单价 |
| `productQuantity` | `cartPromotionItem.getQuantity()` | 购买数量 |
| `promotionAmount` | `cartPromotionItem.getReduceAmount()` | 每件商品的活动优惠金额 |
| `promotionName` | `cartPromotionItem.getPromotionMessage()` | 促销说明 |
| `giftIntegration` | `cartPromotionItem.getIntegration()` | 每件商品赠送积分 |
| `giftGrowth` | `cartPromotionItem.getGrowth()` | 每件商品赠送成长值 |

#### 六、判断库存是否足够

```java
if (!hasStock(cartPromotionItemList)) {
    Asserts.fail("库存不足，无法下单");
}
```

对应辅助函数：

```java
private boolean hasStock(List<CartPromotionItem> cartPromotionItemList) {
    for (CartPromotionItem cartPromotionItem : cartPromotionItemList) {
        if (cartPromotionItem.getRealStock() == null || cartPromotionItem.getRealStock() <= 0) {
            return false;
        }
    }
    return true;
}
```

这一步只是一个简单库存校验：只要 `realStock` 为空，或者 `realStock <= 0`，就不允许下单。

需要注意一个源码细节：这里没有判断 `realStock >= quantity`，只是判断真实库存是否大于 0。所以它更像是一个粗略校验，真正的库存数量锁定还要看后面的 `lockStock(...)` 和商品服务自己的库存逻辑。

#### 七、处理优惠券

源码先判断用户有没有传 `couponId`：

```java
if (orderParam.getCouponId() == null) {
    for (OmsOrderItem orderItem : orderItemList) {
        orderItem.setCouponAmount(new BigDecimal(0));
    }
} else {
    SmsCouponHistoryDetail couponHistoryDetail =
            getUseCoupon(cartPromotionItemList, orderParam.getCouponId());
    if (couponHistoryDetail == null) {
        Asserts.fail("该优惠券不可用");
    }
    handleCouponAmount(orderItemList, couponHistoryDetail);
}
```

分两种情况：

1. 不使用优惠券：每个订单商品的 `couponAmount` 都设置为 0。
2. 使用优惠券：先校验优惠券是否真的可用，再把优惠券金额分摊到订单商品上。

这里不是简单地相信前端传来的 `couponId`，而是调用：

```java
private SmsCouponHistoryDetail getUseCoupon(List<CartPromotionItem> cartPromotionItemList, Long couponId) {
    List<SmsCouponHistoryDetail> couponHistoryDetailList =
        couponFeign.listCartPromotion(1, cartPromotionItemList, memberUtil.getRedisUmsMember(request).getId()).getData();
    for (SmsCouponHistoryDetail couponHistoryDetail : couponHistoryDetailList) {
        if (couponHistoryDetail.getCoupon().getId().equals(couponId)) {
            return couponHistoryDetail;
        }
    }
    return null;
}
```

意思是：

1. 根据当前购物车商品和当前会员，调用优惠券服务重新查询“本单可用优惠券”。
2. 在可用优惠券列表里找前端传来的 `couponId`。
3. 找得到才允许使用，找不到就说明不可用。

这一步是典型的服务端防伪校验：前端可以传一个优惠券 ID，但这个优惠券到底能不能用，必须由后端重新判断。

#### 八、优惠券金额如何分摊

处理优惠券的函数是：

```java
private void handleCouponAmount(List<OmsOrderItem> orderItemList,
                                SmsCouponHistoryDetail couponHistoryDetail) {
    SmsCoupon coupon = couponHistoryDetail.getCoupon();
    if (coupon.getUseType().equals(0)) {
        calcPerCouponAmount(orderItemList, coupon);
    } else if (coupon.getUseType().equals(1)) {
        List<OmsOrderItem> couponOrderItemList =
                getCouponOrderItemByRelation(couponHistoryDetail, orderItemList, 0);
        calcPerCouponAmount(couponOrderItemList, coupon);
    } else if (coupon.getUseType().equals(2)) {
        List<OmsOrderItem> couponOrderItemList =
                getCouponOrderItemByRelation(couponHistoryDetail, orderItemList, 1);
        calcPerCouponAmount(couponOrderItemList, coupon);
    }
}
```

优惠券有三种使用范围：

| `useType` | 含义 | 处理方式 |
| --- | --- | --- |
| `0` | 全场通用 | 所有订单商品一起分摊优惠券 |
| `1` | 指定分类 | 只让命中分类的商品分摊优惠券 |
| `2` | 指定商品 | 只让命中商品的订单项分摊优惠券 |

指定分类或指定商品时，会进入 `getCouponOrderItemByRelation(...)`，把不符合条件的订单商品 `couponAmount` 设置为 0，只让符合条件的商品参与优惠券分摊。

真正分摊金额的函数是：

```java
private void calcPerCouponAmount(List<OmsOrderItem> orderItemList, SmsCoupon coupon) {
    BigDecimal totalAmount = calcTotalAmount(orderItemList);
    for (OmsOrderItem orderItem : orderItemList) {
        BigDecimal couponAmount = orderItem.getProductPrice()
                .divide(totalAmount, 3, RoundingMode.HALF_EVEN)
                .multiply(coupon.getAmount());
        orderItem.setCouponAmount(couponAmount);
    }
}
```

它的思路是：

```text
某个商品分到的优惠券金额 = 商品单价 / 可用商品总金额 * 优惠券面额
```

这里设置到 `orderItem.couponAmount` 上的是“每件商品分摊到的优惠券金额”。后面计算订单总优惠券金额时，会再乘以购买数量。

需要注意一个源码细节：这里分摊时用的是 `productPrice`，不是 `productPrice * quantity`。如果多个商品购买数量差异很大，这个分摊结果可能不够严谨。按更常见的设计，应该用每个订单项的小计金额参与比例计算。

#### 九、处理积分抵扣

积分逻辑和优惠券类似，先判断用户有没有使用积分：

```java
if (orderParam.getUseIntegration() == null || orderParam.getUseIntegration().equals(0)) {
    for (OmsOrderItem orderItem : orderItemList) {
        orderItem.setIntegrationAmount(new BigDecimal(0));
    }
} else {
    BigDecimal totalAmount = calcTotalAmount(orderItemList);
    BigDecimal integrationAmount = getUseIntegrationAmount(
            orderParam.getUseIntegration(),
            totalAmount,
            currentMember,
            orderParam.getCouponId() != null);
    if (integrationAmount.compareTo(new BigDecimal(0)) == 0) {
        Asserts.fail("积分不可用");
    } else {
        for (OmsOrderItem orderItem : orderItemList) {
            BigDecimal perAmount = orderItem.getProductPrice()
                    .divide(totalAmount, 3, RoundingMode.HALF_EVEN)
                    .multiply(integrationAmount);
            orderItem.setIntegrationAmount(perAmount);
        }
    }
}
```

分两种情况：

1. 不使用积分：每个订单商品的 `integrationAmount` 设置为 0。
2. 使用积分：先算出这些积分能抵扣多少钱，再把抵扣金额分摊到每个订单商品上。

积分校验函数是：

```java
private BigDecimal getUseIntegrationAmount(Integer useIntegration,
                                           BigDecimal totalAmount,
                                           UmsMember currentMember,
                                           boolean hasCoupon) {
    BigDecimal zeroAmount = new BigDecimal(0);
    if (useIntegration.compareTo(currentMember.getIntegration()) > 0) {
        return zeroAmount;
    }
    UmsIntegrationConsumeSetting integrationConsumeSetting =
            memberFeign.integrationConsumeSetting(1L).getData();
    if (hasCoupon && integrationConsumeSetting.getCouponStatus().equals(0)) {
        return zeroAmount;
    }
    if (useIntegration.compareTo(integrationConsumeSetting.getUseUnit()) < 0) {
        return zeroAmount;
    }
    BigDecimal integrationAmount = new BigDecimal(useIntegration)
            .divide(new BigDecimal(integrationConsumeSetting.getUseUnit()), 2, RoundingMode.HALF_EVEN);
    BigDecimal maxPercent = new BigDecimal(integrationConsumeSetting.getMaxPercentPerOrder())
            .divide(new BigDecimal(100), 2, RoundingMode.HALF_EVEN);
    if (integrationAmount.compareTo(totalAmount.multiply(maxPercent)) > 0) {
        return zeroAmount;
    }
    return integrationAmount;
}
```

校验规则有四个：

1. 用户使用的积分不能超过自己拥有的积分。
2. 如果已经用了优惠券，要看积分规则是否允许“积分和优惠券同时使用”。
3. 使用积分要达到最低使用单位 `useUnit`。
4. 积分抵扣金额不能超过订单金额允许抵扣的最大百分比。

积分抵扣金额的计算方式是：

```text
积分抵扣金额 = 使用积分数 / useUnit
```

例如 `useUnit = 100`，用户使用 `200` 积分，则抵扣金额是 `2`。

通过校验之后，源码把积分抵扣金额按商品单价占比分摊到每个订单商品上：

```text
某个商品分到的积分抵扣 = 商品单价 / 订单总金额 * 积分总抵扣金额
```

这里和优惠券分摊一样，也是用 `productPrice`，不是 `productPrice * quantity`，所以数量差异较大时也可能存在分摊不严谨的问题。

#### 十、计算订单商品实付金额

```java
handleRealAmount(orderItemList);
```

对应函数：

```java
private void handleRealAmount(List<OmsOrderItem> orderItemList) {
    for (OmsOrderItem orderItem : orderItemList) {
        BigDecimal realAmount = orderItem.getProductPrice()
                .subtract(orderItem.getPromotionAmount())
                .subtract(orderItem.getCouponAmount())
                .subtract(orderItem.getIntegrationAmount());
        orderItem.setRealAmount(realAmount);
    }
}
```

每个订单商品的实付单价：

```text
realAmount = 商品单价 - 活动优惠 - 优惠券抵扣 - 积分抵扣
```

这里的 `realAmount` 是订单明细维度的单件实付金额，不是整单支付金额。

#### 十一、锁定库存

```java
lockStock(cartPromotionItemList);
```

对应函数：

```java
private void lockStock(List<CartPromotionItem> cartPromotionItemList) {
    for (CartPromotionItem cartPromotionItem : cartPromotionItemList) {
        productFeign.lockStock(cartPromotionItem.getProductSkuId(), cartPromotionItem.getQuantity());
    }
}
```

这一步会调用商品服务锁定 SKU 库存。下单后还没有支付，所以不是立即扣减真实库存，而是先锁定库存，防止其他用户继续购买这部分库存。

后续如果支付成功，`paySuccess` 会把锁定库存转成真实库存扣减；如果订单超时取消，则取消订单逻辑会释放锁定库存。

#### 十二、创建订单主表对象 OmsOrder

接下来开始组装订单主表数据：

```java
OmsOrder order = new OmsOrder();
order.setDiscountAmount(new BigDecimal(0));
order.setTotalAmount(calcTotalAmount(orderItemList));
order.setFreightAmount(new BigDecimal(0));
order.setPromotionAmount(calcPromotionAmount(orderItemList));
order.setPromotionInfo(getOrderPromotionInfo(orderItemList));
```

几个金额字段含义：

| 字段 | 含义 |
| --- | --- |
| `discountAmount` | 管理员后台调整订单使用的折扣，这里默认为 0 |
| `totalAmount` | 商品总金额，未扣促销、优惠券、积分 |
| `freightAmount` | 运费，这里写死为 0 |
| `promotionAmount` | 商品促销总金额 |
| `promotionInfo` | 订单促销说明，多个商品促销名用分号拼接 |

订单商品总金额的计算：

```java
private BigDecimal calcTotalAmount(List<OmsOrderItem> orderItemList) {
    BigDecimal totalAmount = new BigDecimal("0");
    for (OmsOrderItem item : orderItemList) {
        totalAmount = totalAmount.add(
            item.getProductPrice().multiply(new BigDecimal(item.getProductQuantity()))
        );
    }
    return totalAmount;
}
```

也就是：

```text
totalAmount = sum(商品单价 * 购买数量)
```

促销总金额的计算：

```java
promotionAmount = sum(每件促销优惠 * 购买数量)
```

#### 十三、设置订单上的优惠券和积分金额

优惠券金额：

```java
if (orderParam.getCouponId() == null) {
    order.setCouponAmount(new BigDecimal(0));
} else {
    order.setCouponId(orderParam.getCouponId());
    order.setCouponAmount(calcCouponAmount(orderItemList));
}
```

积分金额：

```java
if (orderParam.getUseIntegration() == null) {
    order.setIntegration(0);
    order.setIntegrationAmount(new BigDecimal(0));
} else {
    order.setIntegration(orderParam.getUseIntegration());
    order.setIntegrationAmount(calcIntegrationAmount(orderItemList));
}
```

这里要区分几个字段：

| 字段 | 含义 |
| --- | --- |
| `couponId` | 本订单使用的优惠券 ID |
| `couponAmount` | 本订单优惠券总抵扣金额 |
| `integrationAmount` | 本订单积分总抵扣金额 |
| `integration` | 源码里先临时设置为“使用积分数”，后面又被改成“赠送积分” |

这里有一个值得记的源码细节：

```java
order.setIntegration(orderParam.getUseIntegration());
```

后面又会执行：

```java
order.setIntegration(calcGifIntegration(orderItemList));
```

也就是说，`order.integration` 最终会变成“购买商品赠送的积分”，不是“用户本次使用的积分”。用户本次使用的积分应该放在 `useIntegration` 字段里，但源码是在 `orderMapper.insert(order)` 之后才执行 `order.setUseIntegration(...)`，没有再 update，所以这个字段是否能入库要特别注意。

#### 十四、计算订单应付金额 payAmount

```java
order.setPayAmount(calcPayAmount(order));
```

对应函数：

```java
private BigDecimal calcPayAmount(OmsOrder order) {
    BigDecimal payAmount = order.getTotalAmount()
            .add(order.getFreightAmount())
            .subtract(order.getPromotionAmount())
            .subtract(order.getCouponAmount())
            .subtract(order.getIntegrationAmount());
    return payAmount;
}
```

整单应付金额：

```text
payAmount = 商品总金额 + 运费 - 活动优惠 - 优惠券优惠 - 积分抵扣
```

这就是用户最终需要支付的钱。

#### 十五、设置订单基础信息

```java
order.setMemberId(currentMember.getId());
order.setCreateTime(new Date());
order.setMemberUsername(currentMember.getUsername());
order.setPayType(orderParam.getPayType());
order.setSourceType(1);
order.setStatus(0);
order.setOrderType(0);
```

含义：

| 字段 | 含义 |
| --- | --- |
| `memberId` | 当前会员 ID |
| `createTime` | 下单时间 |
| `memberUsername` | 当前会员用户名 |
| `payType` | 支付方式 |
| `sourceType` | 订单来源，`0->PC订单；1->app订单`，这里固定为 1 |
| `status` | 订单状态，0 表示待付款 |
| `orderType` | 订单类型，0 表示正常订单，1 表示秒杀订单 |

订单刚创建时状态为 `0`，也就是待付款。

#### 十六、查询并设置收货地址

```java
UmsMemberReceiveAddress address =
        memberFeign.getItem(orderParam.getMemberReceiveAddressId()).getData();
order.setReceiverName(address.getName());
order.setReceiverPhone(address.getPhoneNumber());
order.setReceiverPostCode(address.getPostCode());
order.setReceiverProvince(address.getProvince());
order.setReceiverCity(address.getCity());
order.setReceiverRegion(address.getRegion());
order.setReceiverDetailAddress(address.getDetailAddress());
```

这里根据 `memberReceiveAddressId` 调用会员服务查询收货地址，然后把地址信息复制到订单表。

为什么订单表要保存一份收货地址快照？

因为用户下单后可能修改或删除自己的收货地址，但已经生成的订单收货地址不能跟着变化，所以订单必须保存下单时的地址信息。

#### 十七、设置确认状态、删除状态、赠送积分成长值和订单号

```java
order.setConfirmStatus(0);
order.setDeleteStatus(0);
order.setIntegration(calcGifIntegration(orderItemList));
order.setGrowth(calcGiftGrowth(orderItemList));
order.setOrderSn(generateOrderSn(order));
```

含义：

| 字段 | 含义 |
| --- | --- |
| `confirmStatus` | 确认收货状态，0 表示未确认 |
| `deleteStatus` | 删除状态，0 表示未删除 |
| `integration` | 购买商品赠送的总积分 |
| `growth` | 购买商品赠送的总成长值 |
| `orderSn` | 订单编号 |

赠送积分的计算逻辑：

```java
sum += orderItem.getGiftIntegration() * orderItem.getProductQuantity();
```

赠送成长值也是类似：

```java
sum += orderItem.getGiftGrowth() * orderItem.getProductQuantity();
```

订单编号由 `generateOrderSn(order)` 生成，订单号一般会包含时间、会员信息或随机因子，用于对外展示和支付对账。

#### 十八、设置自动确认收货天数

```java
List<OmsOrderSetting> orderSettings =
        orderSettingMapper.selectByExample(new OmsOrderSettingExample());
if (CollUtil.isNotEmpty(orderSettings)) {
    order.setAutoConfirmDay(orderSettings.get(0).getConfirmOvertime());
}
```

这里从 `oms_order_setting` 订单设置表里取配置，把自动确认收货天数写入订单。

如果后续订单发货后，超过这个天数用户还没有确认收货，系统就可以自动确认收货。

#### 十九、插入订单主表和订单商品表

```java
orderMapper.insert(order);
for (OmsOrderItem orderItem : orderItemList) {
    orderItem.setOrderId(order.getId());
    orderItem.setOrderSn(order.getOrderSn());
}
orderItemDao.insertList(orderItemList);
```

这一步正式落库：

1. 先插入 `oms_order` 主表。
2. 插入成功后，`order.getId()` 会拿到数据库生成的订单 ID。
3. 再把订单 ID 和订单编号设置到每个 `OmsOrderItem`。
4. 批量插入 `oms_order_item` 明细表。

订单主表和订单明细表的关系：

```text
oms_order.id = oms_order_item.order_id
oms_order.order_sn = oms_order_item.order_sn
```

#### 二十、更新优惠券使用状态

```java
if (orderParam.getCouponId() != null) {
    couponFeign.updateCouponStatus(orderParam.getCouponId(), currentMember.getId(), 1);
}
```

如果本次订单使用了优惠券，就调用优惠券服务把优惠券状态改为已使用。

这里的 `1` 表示已使用状态。

#### 二十一、扣减会员积分

```java
if (orderParam.getUseIntegration() != null) {
    order.setUseIntegration(orderParam.getUseIntegration());
    memberFeign.updateIntegration(currentMember.getId(), -orderParam.getUseIntegration());
}
```

如果用户使用了积分，就调用会员服务扣减积分。

注意两个源码细节：

1. 判断条件是 `useIntegration != null`，所以如果前端传了 `0`，也会调用一次扣减 `0` 积分。
2. `order.setUseIntegration(...)` 在 `orderMapper.insert(order)` 后面才执行，源码中没有再更新订单表，所以这个字段可能只是返回给前端的 `order` 对象有值，但数据库里的 `use_integration` 不一定被保存。

#### 二十二、删除购物车中的下单商品

```java
deleteCartItemList(cartPromotionItemList, currentMember);
```

对应函数会整理出这次下单的购物车商品 ID，然后删除这些购物车记录。

这一步表示：商品已经生成订单了，就不应该继续留在购物车里。

#### 二十三、发送延迟取消订单消息

```java
sendDelayMessageCancelOrder(order.getId());
```

下单成功后，系统会发送一条延迟消息。订单如果长时间未支付，消息到期后会触发取消订单逻辑。

这个设计是电商里常见的“超时未支付自动取消订单”：

```text
创建订单 -> 发送延迟消息 -> 到期检查订单状态
如果仍然待付款 -> 取消订单 -> 释放锁定库存 -> 返还优惠券/积分
```

#### 二十四、返回结果

```java
Map<String, Object> result = new HashMap<>();
result.put("order", order);
result.put("orderItemList", orderItemList);
return result;
```

最终返回给前端两个对象：

| key | 含义 |
| --- | --- |
| `order` | 订单主表信息 |
| `orderItemList` | 订单商品明细列表 |

Controller 外层会包装成：

```java
CommonResult.success(result, "下单成功")
```

#### 二十五、整体流程梳理

完整流程可以按这个顺序记：

```text
1. 接收 OrderParam
2. 获取当前会员
3. 根据 cartIds 查询购物车商品并重新计算促销
4. 把 CartPromotionItem 转成 OmsOrderItem
5. 校验库存
6. 校验并分摊优惠券
7. 校验并分摊积分抵扣
8. 计算每个订单商品的实付金额
9. 锁定库存
10. 组装订单主表 OmsOrder
11. 计算订单总金额、促销金额、优惠券金额、积分抵扣金额、应付金额
12. 设置会员信息、支付方式、订单状态、收货地址
13. 设置赠送积分、成长值、订单号、自动确认收货天数
14. 插入 oms_order
15. 插入 oms_order_item
16. 更新优惠券为已使用
17. 扣减会员积分
18. 删除已下单的购物车商品
19. 发送延迟消息，用于超时取消订单
20. 返回订单和订单明细
```

#### 二十六、金额字段重点区分

| 字段 | 所在对象 | 含义 |
| --- | --- | --- |
| `productPrice` | `OmsOrderItem` | 商品下单单价 |
| `productQuantity` | `OmsOrderItem` | 购买数量 |
| `promotionAmount` | `OmsOrderItem` | 每件商品的促销优惠 |
| `couponAmount` | `OmsOrderItem` | 每件商品分摊到的优惠券金额 |
| `integrationAmount` | `OmsOrderItem` | 每件商品分摊到的积分抵扣金额 |
| `realAmount` | `OmsOrderItem` | 每件商品最终实付单价 |
| `totalAmount` | `OmsOrder` | 整单商品总金额 |
| `promotionAmount` | `OmsOrder` | 整单促销优惠总额 |
| `couponAmount` | `OmsOrder` | 整单优惠券抵扣总额 |
| `integrationAmount` | `OmsOrder` | 整单积分抵扣总额 |
| `payAmount` | `OmsOrder` | 整单最终应付金额 |

两个核心公式：

```text
订单明细实付单价 = 商品单价 - 活动优惠 - 优惠券抵扣 - 积分抵扣
```

```text
订单应付金额 = 商品总金额 + 运费 - 活动优惠总额 - 优惠券优惠总额 - 积分抵扣总额
```

#### 二十七、这个函数里容易忽略的源码细节

1. 下单时会重新调用 `cartItemService.listPromotion(...)` 计算促销，不直接相信前端价格。
2. 优惠券会通过 `couponFeign.listCartPromotion(...)` 重新查询可用优惠券，不直接相信前端传来的 `couponId`。
3. 积分会校验会员积分余额、最低使用单位、是否能和优惠券共用、最大抵扣比例。
4. 库存先校验 `realStock > 0`，再调用商品服务锁定库存。
5. 下单时是锁库存，不是直接扣真实库存。
6. 支付成功后才会真正扣减库存，超时取消时会释放锁定库存。
7. 订单保存的是商品、促销、收货地址快照，避免后续商品或地址修改影响历史订单。
8. `order.integration` 在源码里先被当成“使用积分”，后面又被覆盖成“赠送积分”，这个字段语义要特别小心。
9. `order.setUseIntegration(...)` 在插入订单之后才执行，源码中没有再次 update，可能导致使用积分数字没有真正保存到订单表。
10. 优惠券和积分分摊时使用的是商品单价占比，不是订单项小计占比，数量差异较大时可能有金额分摊不严谨的问题。
11. 这个函数里没有看到 `@Transactional`，但它会同时写订单、锁库存、改优惠券、扣积分、删购物车、发 MQ 消息，真实业务里要重点关注分布式一致性问题。

#### 二十八、最终记忆版

`generateOrder` 的本质不是“新增一条订单”这么简单，而是一次完整的下单事务：

```text
服务端重新算价格
-> 校验优惠券和积分
-> 计算订单金额
-> 锁库存
-> 写订单和订单明细
-> 修改优惠券、积分、购物车
-> 发延迟消息处理超时未支付
```

所以学习这个函数时，不要只盯着 `orderMapper.insert(order)`。真正重要的是：它把购物车、商品促销、优惠券、积分、库存、会员地址、订单状态、延迟取消这些模块串成了一条完整的下单链路。
